name: eduvl_ai
description: "EduVL-AI Flutter project with 3D models."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_cube: ^0.1.1

  firebase_core: ^3.4.0
  cloud_firestore: ^5.4.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

